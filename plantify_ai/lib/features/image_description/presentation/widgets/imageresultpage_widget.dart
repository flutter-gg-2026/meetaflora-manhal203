 import 'package:flutter/material.dart';

class ImageresultpageWidget extends StatelessWidget {
  const ImageresultpageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
  }
